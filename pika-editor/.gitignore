node_modules/
.DS_Store
*.log
.last-project
.last-project-*
.pae-server.lock

# Mocks are dev-time screenshots — keep the HTML + render scripts, drop the PNGs
_mocks/*.png

# Project artifacts — user-generated content per project. Each project
# directory ALSO has its own nested .git/ for named-save history, which
# would conflict with the top-level repo if tracked here. Exclude the
# whole tree; per-project versioning stays self-contained.
projects/

# Per-machine agent runtime config (active Claude model). Not source.
server/agent-config.json

# Editor scratch
.vite/
dist/
.tsbuildinfo

# Codex CLI session scratch
.codex
.codex/

# Env
server/.env
.env
.env.local
